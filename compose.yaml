include:
#  - notifications_api/compose.yml
#  - kafka_cluster/compose.yml
  - admin_panel/compose.yaml


services:
  postgres:
    image: postgres:16.1
    restart: always
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

#  redis:
#    image: redis:7.2.5
#    restart: always
#    env_file: .env
#    # ...
#
#  nginx:
#    image: nginx:1.25.5
#    ports:
#      - "80:80"
#    volumes:
#      - ./nginx.conf:/etc/nginx/nginx.conf:ro
#      - ugc_nginx_logs:/var/log/nginx
#    depends_on:
#      - admin

volumes:
  pg_data:
    driver: local