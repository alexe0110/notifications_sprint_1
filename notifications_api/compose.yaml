services:
  notifications_api:
    build:
      context: .
    ports:
      - "8001:8000"
    env_file: ../.env
  nginx:
    build: ./nginx/
    ports:
    - 80:80
    depends_on:
    - notifications_api