services:
  admin_panel:
    build:
      context: .
#    ports:
#      - "8000:8000"
    env_file:
      - ../.env
    volumes:
      - static_volume:/opt/app/static
    command: ./run.sh


volumes:
  static_volume:
    driver: local